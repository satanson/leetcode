#include<cppstdlib.hpp>
#include<tree_serialize.hpp>

class Solution{
public:
	void recoverTree(TreeNode *root){

	}
	void findPath(TreeNode* root,list<TreeNode*> &path,vector<TreeNode*> &paths){
		if (!root)return;
		path.push_back(root);
		if (root->left && root->right){
		}else if (root->
	}

};
